{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Make",
      "type": "process",
      "command": "C:\\c stuff\\usr\\bin\\bash.exe",
      "args": [
        "-lc",
        "cd \"$(cygpath -u \"${workspaceFolder}\")\" && make clean && make"
      ],
      "options": {
        "cwd": "${workspaceFolder}",
        "env": {
          "MSYSTEM": "MINGW64",
          "PATH": "C:\\c stuff\\mingw64\\bin;${env:PATH}"
        }
      },
      "group": { "kind": "build", "isDefault": true },
      "problemMatcher": ["$gcc"]
    }
  ]
}
